import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#ececec',
  },
  inner_container: {
    backgroundColor: '#fff',
    marginBottom: 10,
    borderRadius: 10,
  },
  button_container: {
    alignItems: 'center',
    padding: 10,
    borderRadius: 10,
  },
});
